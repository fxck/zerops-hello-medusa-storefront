zerops:
  - setup: storefront
    build:
      base: nodejs@18
      buildCommands:
        - npm i
        - npm run build
      cache:
        - ./node_modules
        - ./package-json.lock
      deploy:
        - ./.next
        - ./node_modules
        - package.json
    run:
      start: yarn start
